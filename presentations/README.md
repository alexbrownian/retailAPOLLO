# Presentations

Slide decks, one-pagers and demo recordings that describe the project.
Nothing in this folder is read by the pipeline or the dashboard.

Large binaries (`.pptx`, `.pdf`, recordings) can be committed, but keep an
eye on repository size; anything over a few tens of megabytes is better
stored alongside the repository than inside it.
